# Local mode (default)
# MCP_TRANSPORT=stdio

# Remote/public mode
MCP_TRANSPORT=streamable-http
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Required for remote mode
MCP_API_KEY=change-me-to-a-long-random-secret
